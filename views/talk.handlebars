<h2>Talks</h2>
<h5>{{talk.name}}</h5>
<h5>{{talk.description}}</h5>
<p>Talk will be given by: {{talk.user.name}} on {{talk.date}}</p>

{{#if talk.questions}}
    {{#each talk.questions}}
        <h5>Question</h5>
        <h5>{{question}}</h5>
        <p>Posted By: {{user.name}} on {{created}}</p>
    {{/each}}
{{/if}}

{{#if talk.attends}}
    {{#each talk.attends}}
        <h5>Attendance</h5>
        <h5>{{user.name}}</h5>
    {{/each}}
{{/if}}